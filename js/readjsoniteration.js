const addLiTag3 = (idx3) => {
    let li3 = `<li>Fizz: ${idx3}</li>`;
    document.getElementById()
}

const divisibleThree = (idx3) => {
    let total = 0;
    let count = 0;
    for (let idx3 = 1; i <= 50; idx3++) {
        if (idx3 % 3 === 0) {
            addLiTag3(idx3);
            total += idx3;
            count++;
        }
    }
const divisibleFive(idx5) {
        let total5 = 0;
        let count5 = 0;
        for (let idx5 = 1; i <= 50; idx5++) {
            if (idx5 % 5 === 0) {
                addLiTag5(idx5);
                total += idx5;
                count++;
            }

        }
    }

}